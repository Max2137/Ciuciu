using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ArmorDefensePassive : MonoBehaviour
{
    public float damageAbsortion = 5f; // Przyk³adowa wartoœæ redukcji obra¿eñ

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}