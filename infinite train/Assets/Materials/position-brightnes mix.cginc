float3 CombinePixelateAndGrayscale(float2 pixelUV, float brightness)
{
    // Użyj współrzędnych UV z pixelizacji i jasności z czarno-białego efektu
    return float3(brightness, brightness, brightness);
}
